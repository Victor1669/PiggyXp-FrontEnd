import { GlobalFontColors } from "@Assets/Colors";
import { StyleSheet } from "react-native";

export const LoadingLevelContainerStyles = StyleSheet.create({
  text: {
    textAlign: "center",
    color: GlobalFontColors.Dark,
    fontWeight: "bold",
  },
});
