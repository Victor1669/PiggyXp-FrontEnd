import { StyleSheet } from "react-native";

export const CodeVerifierStyles = StyleSheet.create({
  textContainer: {
    flexDirection: "row",
    gap: 10,
    marginHorizontal: "auto",
  },
  text: {
    color: "#fff",
  },
});
