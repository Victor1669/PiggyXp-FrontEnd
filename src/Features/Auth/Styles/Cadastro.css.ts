import { StyleSheet } from "react-native";

export const CadastroStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  formContainer: {
    marginTop: 40,
    gap: 20,
  },
  jaTemConta: {
    margin: 25,
    flexDirection: "row",
  },
});
