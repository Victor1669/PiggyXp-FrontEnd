import { StyleSheet } from "react-native";

export const CadastroStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  formContainer: {
    marginTop: 40,
    gap: 20,
  },
  jaTemConta: {
    margin: 25,
    marginTop: 5,
    flexDirection: "row",
  },
});
