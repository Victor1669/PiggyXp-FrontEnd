import { StyleSheet } from "react-native";

export const LoginStyles = StyleSheet.create({
  formContainer: {
    marginTop: 40,
    gap: 20,
  },
  naoTemConta: {
    flexDirection: "row",
    margin: 30,
  },
});
